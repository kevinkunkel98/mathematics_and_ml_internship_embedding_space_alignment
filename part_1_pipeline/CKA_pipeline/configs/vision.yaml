# configs/dinov2.yaml
model_id: "facebook/dinov2-large"
m_type: "vision"
probe_layer: 20
hidden_dim: 1024
# Pfad zum trainierten Phase-1-Vision-Checkpoint (best_model.pt).
# Leer lassen ("") -> es wird nur das vortrainierte DINOv2 genutzt.
path: ""
lora:
  lora_r: 8
  lora_alpha: 16
  target_modules: ["query", "value"]